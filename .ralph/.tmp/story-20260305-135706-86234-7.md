### US-007: Backfill tests for REST loader and route domains
Status: in_progress
Depends on: US-001, US-002, US-004

Description:
As a maintainer, I want unit-level route registration and handler tests for REST domains so that protected API contracts remain unchanged.

Acceptance Criteria:
- [ ] Cover src/loaders/RESTLoader.ts, src/loaders/rest/*, and route modules under src/components/overlay, src/components/admin, and src/components/ingest.
- [ ] Validate route registration and key handler responses with Fastify injection-style tests or equivalent isolated handler tests.
- [ ] Example: valid POST /overlay/pair/consume returns the expected status and response payload contract.
- [ ] Negative case: malformed consume payload returns existing validation error status/body shape.
- [ ] Protected routes assert auth guard behavior for valid and invalid credentials.
